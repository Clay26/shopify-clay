<h1>Products</h1>

<ul>
  <% @products.each do |product| %>
    <li>
      <a href="<%= product_path(product)%>">
        <%= product.name %>
      </a>
    </li>
  <% end %>
</ul>

<a href="<%= new_product_path() %>">Create</a>